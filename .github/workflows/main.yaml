name: Manual Events
on: 
#  schedule:
#    - cron: '*/5 * * * *' #Every 5 min
#    push:
#      branches:
#        - main
#  workflow_dispatch:
#  issues:
#    types:
#      - opened
  workflow_dispatch:
    inputs:
      param:
        description: "Description of param1"
        required: true
        default: "hello world"
jobs:
  show-info:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Info
        run: |
          echo "Running manual tasks with param1: ${{github.event.inputs.param1}}"
